package org.example.models.vehicles;


import java.awt.Point;

public class Truck extends Vehicle {
    public Truck(Point destination, Point position, int speed, int vehicleType) {
        super(destination, position, speed, vehicleType);
    }
}
