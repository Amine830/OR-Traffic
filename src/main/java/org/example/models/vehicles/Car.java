package org.example.models.vehicles;

import java.awt.Point;

public class Car extends Vehicle {
    public Car(Point destination, Point position, int speed) {
        super(destination, position, speed);
    }
}